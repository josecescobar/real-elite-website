node_modules
.next
.vercel
.env
.env.local
coverage

# OS / editor noise
.DS_Store

# Generated TypeScript incremental build cache
tsconfig.tsbuildinfo

# next-sitemap output (regenerated by `npm run build` via postbuild)
/public/sitemap.xml
/public/sitemap-*.xml
/public/robots.txt

# image originals kept locally, not deployed
*.backup
