node_modules
.next
.vercel
.env
.env.local

# OS junk
.DS_Store

# generated by next-sitemap on postbuild
/public/sitemap*.xml
/public/robots.txt

# image originals kept locally, not deployed
*.backup

# TypeScript incremental build cache — regenerated by `tsc`
*.tsbuildinfo

# Source image library — kept locally, not deployed (curated, optimized copies live in public/images)
/images/
